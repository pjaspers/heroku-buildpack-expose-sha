#!/usr/bin/env bash
# Usage: bin/compile <build-dir> <cache-dir> <env-dir>

env_dir=$3
echo $SOURCE_VERSION
if [ -d "$env_dir" ]; then
    echo "Exporting SHA=$SOURCE_VERSION"
    export SHA=$SOURCE_VERSION
else
    echo "No env dir found"
fi
